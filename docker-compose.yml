version: '3.8'

services:
  ticket-generator:
    build: .
    container_name: ticket-generator
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - HOST=0.0.0.0
      - ALLOWED_ORIGIN=*
    restart: always
